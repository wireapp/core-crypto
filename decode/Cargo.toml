[package]
name = "decode"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.20", features = ["derive"] }
proteus-wasm = { workspace = true,  features = ["serde"]}
base64 = { workspace = true }
