-- this table was only relevant pre-CC10, when clients managed e2ei externally.
-- now that CC10 is out, the old way of doing e2ei isn't even available anymore,
-- so this is now just stale data.
DROP TABLE e2ei_enrollment;
