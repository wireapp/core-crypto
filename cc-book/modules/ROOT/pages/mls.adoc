= MLS

// What MLS is (RFC 9420)
// Key insight: group key agreement — one epoch key shared by all members, not bilateral sessions
// The ratchet tree: how membership is represented and updated efficiently
// Epochs: what they are, when they increment, why they matter
// Proposals and commits: the two-phase model for membership changes
// Why MLS scales where Proteus doesn't
// Wire's path forward: E2EI (end-to-end identity) built on MLS credentials

// == CoreCrypto's MLS Interface — Overview

// CoreCrypto as the root context (opaque, cheap to clone, thread-safe)
// The three roles in a typical call: CoreCrypto (read), TransactionContext (write), MlsTransport (deliver)
// Initialization sequence: new() → new_transaction() → mls_init() → finish()
// High-level lifecycle diagram: create group → add members → exchange messages → remove members → wipe
