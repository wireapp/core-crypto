use async_trait::async_trait;
use rusqlite::params;

use crate::{
    CryptoKeystoreResult,
    connection::{KeystoreDatabaseConnection, TransactionWrapper},
    entities::{ConsumerData, E2eiAcmeCA, count_helper, count_helper_tx, delete_helper, get_helper, load_all_helper},
    traits::{EncryptData, Encrypting, Entity, EntityBase, EntityDatabaseMutation, UniqueEntityImplementationHelper},
};

impl EntityBase for E2eiAcmeCA {
    type ConnectionType = KeystoreDatabaseConnection;
    type AutoGeneratedFields = ();
    const COLLECTION_NAME: &'static str = "e2ei_acme_ca";
}

impl UniqueEntityImplementationHelper for E2eiAcmeCA {
    fn new(content: Vec<u8>) -> Self {
        Self { content }
    }
    fn content(&self) -> &[u8] {
        &self.content
    }
}
